{
  "name": "specforge",
  "version": "0.1.0",
  "description": "Spec-first development for Claude Code: /specforge and /specforge-implement skills, a default agent, and hooks that remind the session to compile spec.yaml before implementation.",
  "author": {
    "name": "Marlon Ferrari"
  },
  "homepage": "https://github.com/ferrarimarlon/specforge",
  "repository": "https://github.com/ferrarimarlon/specforge",
  "keywords": [
    "spec",
    "specforge",
    "planning",
    "requirements",
    "claude-code",
    "scope-drift"
  ],
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "\\b(build|implement|create|develop|write|add|make|code|generate)\\b",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[specforge-default] Spec-first policy active. If no spec.yaml exists for this task, /specforge will be invoked before any implementation.'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if ls specforge-bundle/spec.yaml 2>/dev/null | grep -q spec.yaml; then echo '[specforge-default] spec.yaml found — implementation authorized.'; else echo '[specforge-default] WARNING: No spec.yaml detected. Run /specforge before writing code.'; fi"
          }
        ]
      }
    ]
  }
}
